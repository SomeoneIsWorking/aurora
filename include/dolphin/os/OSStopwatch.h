#ifndef DOLPHIN_OS_OSSTOPWATCH_H
#define DOLPHIN_OS_OSSTOPWATCH_H

// Forwarder: OSStopwatch is declared in Aurora's monolithic <dolphin/os.h>.
// Some game decomp (SMS, SPM, etc.) includes the granular subheader directly
// as it existed in the original GameCube SDK layout — resolve to os.h so the
// symbol is available either way.
#include <dolphin/os.h>

#endif
